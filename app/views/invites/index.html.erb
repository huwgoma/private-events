<%= render "failed_invites" %>

<h1>Invite Users to <%= @event.name %></h1>
<%= render "create_invites_form", invite: @invite, invitable_users: @invitable_users %>

<h2>Revoke Invites</h2>
<ul>
    <% @invites.each do |invite| %>
        <li><%= invite.invitee.name %></li>
    <% end %>
</ul>